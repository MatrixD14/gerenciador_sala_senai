RewriteEngine On
RewriteBase /

# Se o arquivo ou pasta existe fisicamente, abre ele direto
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Se NÃO existe, manda para o index.php processar as rotas
RewriteRule ^ index.php [L]